{
  "name": "@unity-index/graph-core",
  "version": "0.5.0",
  "private": true,
  "description": "Shared TS core for unity-index-graph: host-bridge contract, message types, graph model.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^3.2.4"
  }
}
